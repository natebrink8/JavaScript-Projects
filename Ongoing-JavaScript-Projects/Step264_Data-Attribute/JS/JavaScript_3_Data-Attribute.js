function displayType(champion) {
    var championType = champion.getAttribute("data-champion-type"); alert("The " + championType + " are the champions of the " + champion.innerHTML + " division!");
}
